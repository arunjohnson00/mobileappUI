import { makeStyles } from "@mui/styles";

export const gridStyle = makeStyles((theme) => ({
  gridContainer: {
    display: "flex",
    marginBottom: "90px",
    flexWrap: "wrap",
    background: "black",
    color: "white",
  },
}));
